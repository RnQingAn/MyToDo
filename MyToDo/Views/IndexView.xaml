<UserControl x:Class="MyToDo.Views.IndexView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MyToDo.Views" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition />

        </Grid.RowDefinitions>

        <TextBlock Foreground="White" Text="hello，任华哥！今天是2025年12月18日 星期四" FontSize="22"  Grid.Row="0" Margin="15"/>

        <ItemsControl Grid.Row="1" ItemsSource="{Binding TaskBars}">
            <!-- 设置 ItemsPanel -->
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="4" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Background="{Binding Color}" Margin="8" CornerRadius="5">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Effect">
                                            <Setter.Value>
                                                <DropShadowEffect
                                                    Color="#DDDDDD"
                                                    ShadowDepth="1"
                                                    BlurRadius="10" />
                                            </Setter.Value>
                                        </Setter>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <Grid>

                            <StackPanel Margin="20,10">
                                <materialDesign:PackIcon Kind="{Binding Icon}" Width="30" Height="30"  />
                                <TextBlock Text="{Binding Title}" Margin="0,15"  FontSize="15" />
                                <TextBlock Text="{Binding Content}" FontWeight="Bold" FontSize="40" />
                            </StackPanel>

                            <Canvas ClipToBounds="True">
                                <Border Canvas.Top="10" Canvas.Right="-50" 
                                Width="120" Height="120"
                                Background="#FFFFFF"
                                CornerRadius="100"
                                Opacity="0.1" />
                                <Border Canvas.Top="80" Canvas.Right="-30" 
                                 Width="120" Height="120"
                                 Background="#FFFFFF"
                                 CornerRadius="100"
                                 Opacity="0.1" />

                            </Canvas>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <Grid Grid.Row="2" Margin="0,10">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Border Grid.RowSpan="2" Grid.Column="0" Grid.Row="0" Margin="10,0" CornerRadius="5" Opacity="0.1"  Background="#BEBEBE"/>

            <DockPanel Margin="10,0" LastChildFill="False"   Grid.Row="0" Grid.Column="0">
                <TextBlock Margin="10" Foreground="White" Text="待办事项"  FontSize="20" FontWeight="Bold"/>
                <Button Margin="10" Height="30" Width="30" Style="{StaticResource MaterialDesignFloatingActionAccentButton}"  DockPanel.Dock="Right" VerticalAlignment="Top">
                    <materialDesign:PackIcon Kind="Add" />
                </Button>
            </DockPanel>
            <DockPanel  Grid.Row="1" Grid.Column="0" Margin="10,0">
                <ListBox ItemsSource="{Binding ToDoDtos}"
                         ScrollViewer.VerticalScrollBarVisibility="Hidden"
                         HorizontalContentAlignment="Stretch"
                         >
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <DockPanel MaxHeight="80" LastChildFill="False">
                                <ToggleButton DockPanel.Dock="Right"/>
                                <StackPanel >
                                    <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="Bold"/>
                                    <TextBlock Text="{Binding Content}" Margin="0,5" Opacity="0.5"/>
                                </StackPanel>
                            </DockPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
            
            <Border Grid.Column="1" Grid.RowSpan="2" Grid.Row="0"  Margin="10,0"  CornerRadius="5" Opacity="0.1"  Background="#BEBEBE" />
            
            <DockPanel Margin="10,0" LastChildFill="False"  Grid.Row="0" Grid.Column="1">
                <TextBlock Margin="10" Foreground="White" Text="备忘录"  FontSize="20" FontWeight="Bold"/>
                <Button Margin="10" Height="30" Width="30" Style="{StaticResource MaterialDesignFloatingActionAccentButton}"  DockPanel.Dock="Right" VerticalAlignment="Top">
                    <materialDesign:PackIcon Kind="Add" />
                </Button>
            </DockPanel>
            <DockPanel  Grid.Row="1"  Grid.Column="1" Margin="10,0">
                <ListBox ItemsSource="{Binding MemoDtos}" 
                         ScrollViewer.VerticalScrollBarVisibility="Hidden"
                         >
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel MaxHeight="80">
                                <TextBlock Text="{Binding Title}" FontSize="16" FontWeight="Bold"/>
                                <TextBlock Text="{Binding Content}" Margin="0,5" Opacity="0.5"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Grid>
    </Grid>
</UserControl>
